package interfaces;
//Only one method without an argument in interface is used then lambdas expression is used.
//For two method is not allowed.
public interface Lambda_A {
	public void lambda(int x);
	
}
