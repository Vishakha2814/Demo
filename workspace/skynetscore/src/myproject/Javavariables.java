package myproject;

public class Javavariables {
	int data=50;
	//instance variable i.e it is outside of the body of method,
	//it is called instance variable because it`s value is instance specific 
	// and is not shared among instances.
	static int m=100;
	//static variable i.e it cannot be local.
	//by creating a single copy of static variable we can share among all instance of 
	//class.
	//memory allocation for static variable only happens once when class is loaded in
	//the memory.
	public static void main(String[] args) {
	}
}
